services:
  homeassistant:
    container_name: ha_workouts_dev
    image: ghcr.io/home-assistant/home-assistant:stable
    volumes:
      - ./dev/config:/config
      - ./custom_components/ha_workouts:/config/custom_components/ha_workouts
    ports:
      - "8123:8123"
    restart: unless-stopped
